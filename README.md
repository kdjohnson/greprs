# greprs
Reading [The Rust Programming Language](https://doc.rust-lang.org/book/second-edition/). It has a project in which you recreate a simple version of `grep`. I simply added in highlighting of the string that it finds the query match to.
